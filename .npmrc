# Acknowledge xlsx vulnerabilities
# These are acceptable because:
# 1. xlsx is only used in admin dashboard (authenticated access)
# 2. Only admins can upload Excel files
# 3. Files are validated before processing
# 4. This is a controlled internal tool, not public-facing

audit-level=moderate
